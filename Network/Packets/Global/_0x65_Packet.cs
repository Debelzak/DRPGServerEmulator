using DRPGServer.Network.Enum.Global;

namespace DRPGServer.Network.Packets.Global
{
    class _0x65_Packet() : OutPacket((ushort)PACKET_ID._0x65_PACKET)
    {
        protected override void Serialize()
        {
            // This packet is empty
        }
    }
}